- [x] `flush_batch()` 在设置 `_log_queue_has_data = True` 后检查并重新激活调度器
- [x] `flush_batch()` 的调度器激活逻辑与 `_log()` 方法完全一致
- [x] 修复后语法检查通过（py_compile）
- [x] 空闲时调度器不会空转（保持按需调度特性）
- [x] 子进程日志路径能正确激活调度器
- [x] UI 事件日志路径 (`_log()`) 仍正常工作