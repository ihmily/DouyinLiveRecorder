# FAQ - 常见问题

## Q1: 出现 "too many values to unpack (expected 2)" 错误

**问题描述**：启动时出现 `An unexpected error occurred: too many values to unpack (expected 2)`

**原因**：`src/proxy.py` 中的 `_get_proxy_info_linux()` 方法在解析代理环境变量时，假设格式为 `ip:port`，但实际环境变量可能包含协议前缀如 `http://127.0.0.1:8080`，导致 `split(':')` 返回 3 个值。

**解决方案**：已修复，使用 `urlparse` 正确解析代理 URL。

---

## Q2: config.ini 是否会实时读取？

**回答**：是的，`config.ini` 会在每次主循环迭代时重新读取。

`read_config_value` 函数每次调用都会执行 `config_parser.read(config_file)`，从磁盘加载最新配置。

**注意事项**：
- 配置更新在**下一次循环迭代**时生效，不是立即生效
- 如果正在录制中，需要等当前循环周期结束后才会读取新配置
- 循环间隔由 `循环时间(秒)` 设置控制（默认 120 秒）

---

## Q3: TS 到 MP4 的转换发生在什么时机？

**回答**：转换发生在**录制完成后**，在后台线程中执行。

**触发条件**：
1. FFmpeg 进程正常退出（`return_code == 0`）
2. 配置中 `录制完成后自动转为mp4格式` 设为 `是`
3. 保存格式是 TS

**触发时机**：
- 主播下播 → 录制结束 → 立即启动转换线程
- 直播流中断/出错 → 录制结束 → 转换

**重要**：如果开启分段录制，TS 分段文件**不会**在录制过程中逐个转换，而是等整个直播结束后统一转换所有分段。

---

## Q4: 分段录制时，每个分段结束会自动转 MP4 吗？

**回答**：不会。分段录制时有两种模式：

| 配置 | 录制中行为 | 下播后行为 |
|------|-----------|-----------|
| 直接 MP4 分段 | 每段直接输出为 MP4 | 无需转换 |
| TS 分段 + 转 MP4 | 积累多个 TS 文件 | 统一转换所有 TS |

**建议**：如果需要实时可用的 MP4 分段，直接选择 MP4 格式录制，FFmpeg 会使用 `-f segment` 直接输出分段 MP4 文件。
